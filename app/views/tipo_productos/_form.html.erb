<%= form_for(@tipo_producto, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @tipo_producto.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@tipo_producto.errors.count, "error") %> prohibited this tipo_producto from being saved:</h4>

      <ul>
      <% @tipo_producto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :no_tipo_producto, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :no_tipo_producto, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
